{
  "name": "designer-web",
  "version": "0.1.0",
  "private": true,
  "description": "Velbrant Studios, a unit of Velcaryn LLP. Marketing site.",
  "scripts": {
    "dev": "next dev -p 4000",
    "build": "next build",
    "start": "next start -p 4000",
    "lint": "eslint app components",
    "check:emdash": "node scripts/check-em-dash.mjs",
    "check:css": "node scripts/check-css-collisions.mjs",
    "verify": "npm run check:emdash && npm run check:css && npm run check:brand && npm run lint && npm run build",
    "check:brand": "node scripts/check-brand-leak.mjs",
    "check:contrast": "node scripts/check-contrast.mjs"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "motion": "^12.42.2",
    "next": "^16.3.0",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "eslint": "^9",
    "eslint-config-next": "^16.1.6",
    "puppeteer": "^25.8.0"
  }
}
